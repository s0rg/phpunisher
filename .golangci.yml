linters-settings:
  gocritic:
    disabled-checks:
      - singleCaseSwitch
      - commentFormatting
    enabled-tags:
      - performance
      - diagnostic
      - style
      - opinionated

linters:
  enable-all: true
  disable:
    # useless
    - exhaustivestruct
    - gochecknoglobals
    - forcetypeassert
    - tagliatelle
    - testpackage
    - varnamelen
    - forbidigo
    - goerr113
    - gofumpt
    - gomnd
    - gci
    # deprecated
    - interfacer
    - scopelint
    - maligned
    - golint
  fast: false

output:
  format: 'colored-line-number'
  print-issued-lines: true
  print-linter-name: true
